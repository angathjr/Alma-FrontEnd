import 'package:flutter/material.dart';

class GeneralProfileEditScreen extends StatelessWidget {
  const GeneralProfileEditScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        
      ],
    );
  }
}